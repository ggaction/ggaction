# STEP 6 — Regression Facet Scale Primitives

## 진행 상태

- [ ] Shared-scales primitive
- [ ] Independent-x primitive
- [ ] Per-cluster regression numeric fixtures
- [ ] Browser Canvas and high-DPI PNG
- [ ] Gate metadata and exact target chains

Gapminder cluster regression facet의 first two variants를 raw graphical primitives로 작성한다. Primitive는
cell-filtered regression coefficients, interval rows, scale domains, headers와 nested Canvas placement를 explicit
values로 가진다.

Gate artifact는 `.artifacts/test/png/roadmap3/facet-resolution/` 아래 capability hierarchy로 생성한다.
