# STEP 1 — Phase Contract, Inventory and Chart Targets

## 진행 상태

- [x] Phase 2 exact action/capability assignment 고정
- [x] Public degree, radial range와 stored-state contract 확인
- [x] Cars canonical Gate target 확정
- [x] Fashion t-SNE coverage target 확정
- [x] Gate 전 public/runtime boundary 명시

## 구현

Phase 2 direct actions은 `encodeTheta`, `encodeR`, `encodePointRadius`다. Capability assignment는
`polar-point`이며 Phase 3 guide와 Phase 4 line/radar behavior는 포함하지 않는다. Gate 전에는 primitives와
test-local reference calculation만 허용한다.
