# Step 1 — Weighted theta semantic과 pure grammar

## 진행 상태

- [x] count theta baseline과 current arc item grain 조사
- [x] exact option, validation, reassignment 계약 작성
- [x] `weight` semantic path와 strict public type 추가
- [x] pure grouped-sum sector derivation 구현
- [x] count compatibility와 invalid/all-zero 회귀
- [x] immutable state, trace와 selection item 회귀

## 작업

`encodeTheta`가 validation, semantic storage와 scale attachment를 계속 소유한다. Arc grammar는 stored
`aggregate`와 `weight`만 읽어 sector angle을 계산하며 renderer는 최종 concrete path만 그린다.
Count와 sum은 category grouping/order/member identity를 공유하고 값 계산만 달라진다.
