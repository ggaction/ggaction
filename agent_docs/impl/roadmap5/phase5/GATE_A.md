# Gate R5-Exit — Roadmap 5 Integration and Closeout

## Gate state

`planned`

## Review target

Phase 5 center layout과 Roadmap 5의 전체 six-capability surface를 Current contract, declarations, public docs,
examples, renderer and package evidence로 닫는다.

## Required evidence

- Center partition grammar, semantic assignment, area materialization와 scale-domain invariants
- Direct `encodeY({ stack: "center" })` and `encodeColor({ layout: "center" })` equivalence
- Selection/highlight, filter/facet/Canvas/scale/guide replay와 renderer parity
- Time unit, category ordering, moving windows, Tick/Angle와 center area의 stable representative charts
- ACTION_INDEX/planned/current/types/docs/package synchronization and full verification
- Complete verified Gate checkpoint pushed to the Roadmap 5 branch

## Work blocked before approval

Roadmap 5 완료 선언. PR, merge, release, publish와 documentation deployment는 별도 사용자 승인이 필요하다.
