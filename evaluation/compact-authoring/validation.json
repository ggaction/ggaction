{
  "schemaVersion": 1,
  "split": "validation",
  "role": "fresh-one-pass-validation-corpus",
  "tasks": [
    {
      "id": "val-temporal-line-canvas",
      "split": "validation",
      "stratum": "simple",
      "dataset": "temporal-groups-v1",
      "query": "Use a temporal scale for a time series chart, add a vertical axis, and render to canvas.",
      "expected": {
        "constraints": ["chart.line", "scale.time", "guide.yAxis", "renderer.canvas"],
        "plan": [
          { "id": "action.createTimeScale", "options": ["id", "type"] },
          { "id": "action.createLinePlot", "options": ["x", "y"] },
          { "id": "action.createYAxis", "options": [] },
          { "id": "runtime.render", "options": [] }
        ],
        "unresolved": [],
        "fallbacks": []
      }
    },
    {
      "id": "val-filter-scatter-png",
      "split": "validation",
      "stratum": "simple",
      "dataset": "mixed-observations-v1",
      "query": "Filter rows before a point chart with axes and png output.",
      "expected": {
        "constraints": ["chart.scatter", "transform.filter", "guide.axes", "renderer.png"],
        "plan": [
          { "id": "action.filterData", "options": ["id", "field", "predicate"] },
          { "id": "action.createScatterPlot", "options": ["x", "y", "guides"] },
          { "id": "runtime.renderToPNG", "options": [] }
        ],
        "unresolved": [],
        "fallbacks": []
      }
    },
    {
      "id": "val-density-area-axes",
      "split": "validation",
      "stratum": "complex",
      "dataset": "mixed-observations-v1",
      "query": "Create kde data, then a filled area with encode x, encode y, and x and y axes.",
      "expected": {
        "constraints": ["mark.area", "transform.density", "encoding.x", "encoding.y", "guide.axes"],
        "plan": [
          { "id": "action.createDensityData", "options": ["id", "field"] },
          { "id": "action.createAreaMark", "options": [] },
          { "id": "action.encodeX", "options": ["field"] },
          { "id": "action.encodeY", "options": ["field"] },
          { "id": "action.createAxes", "options": [] }
        ],
        "unresolved": [],
        "fallbacks": []
      }
    },
    {
      "id": "val-regression-data-line-svg",
      "split": "validation",
      "stratum": "complex",
      "dataset": "mixed-observations-v1",
      "query": "Derive fitted rows and draw a line plot with group series, then render svg.",
      "expected": {
        "constraints": ["chart.line", "transform.regression", "encoding.group", "renderer.svg"],
        "plan": [
          { "id": "action.createRegressionData", "options": ["id", "x", "y"] },
          { "id": "action.createLinePlot", "options": ["x", "y", "groupBy"] },
          { "id": "runtime.renderToSVG", "options": [] }
        ],
        "unresolved": [],
        "fallbacks": []
      }
    },
    {
      "id": "val-window-line-axes",
      "split": "validation",
      "stratum": "simple",
      "dataset": "temporal-groups-v1",
      "query": "Rank rows with a window transform before a line chart with axes.",
      "expected": {
        "constraints": ["chart.line", "transform.window", "guide.axes"],
        "plan": [
          { "id": "action.createWindowData", "options": ["id", "operations"] },
          { "id": "action.createLinePlot", "options": ["x", "y", "guides"] }
        ],
        "unresolved": [],
        "fallbacks": []
      }
    },
    {
      "id": "val-monthly-bar-color",
      "split": "validation",
      "stratum": "complex",
      "dataset": "temporal-groups-v1",
      "query": "Create a month bucket and a column chart with encode color and axes.",
      "expected": {
        "constraints": ["chart.bar", "transform.timeUnit", "encoding.color", "guide.axes"],
        "plan": [
          { "id": "action.createTimeUnitData", "options": ["id", "field", "unit", "as"] },
          { "id": "action.createBarPlot", "options": ["x", "y", "color", "guides"] }
        ],
        "unresolved": [],
        "fallbacks": []
      }
    },
    {
      "id": "val-interval-point-error-bars",
      "split": "validation",
      "stratum": "complex",
      "dataset": "mixed-observations-v1",
      "query": "Build confidence interval data, point mark, encode x, encode y, error bars, and axes.",
      "expected": {
        "constraints": ["mark.point", "transform.interval", "statistics.errorBar", "encoding.x", "encoding.y", "guide.axes"],
        "plan": [
          { "id": "action.createIntervalData", "options": ["id", "field"] },
          { "id": "action.createPointMark", "options": [] },
          { "id": "action.encodeX", "options": ["field"] },
          { "id": "action.encodeY", "options": ["field"] },
          { "id": "action.createErrorBar", "options": [] },
          { "id": "action.createAxes", "options": [] }
        ],
        "unresolved": [],
        "fallbacks": []
      }
    },
    {
      "id": "val-log-scale-rect",
      "split": "validation",
      "stratum": "simple",
      "dataset": "mixed-observations-v1",
      "query": "Create scale as a log scale, then use rectangle cells with x encoding and y encoding.",
      "expected": {
        "constraints": ["mark.rect", "scale.create", "scale.log", "encoding.x", "encoding.y"],
        "plan": [
          { "id": "action.createLogScale", "options": ["id", "type"] },
          { "id": "action.createScale", "options": ["id"] },
          { "id": "action.createRectMark", "options": [] },
          { "id": "action.encodeX", "options": ["field"] },
          { "id": "action.encodeY", "options": ["field"] }
        ],
        "unresolved": [],
        "fallbacks": []
      }
    },
    {
      "id": "val-color-scale-scatter",
      "split": "validation",
      "stratum": "simple",
      "dataset": "mixed-observations-v1",
      "query": "Add a palette scale and a scatter plot with color encoding and legend.",
      "expected": {
        "constraints": ["chart.scatter", "scale.color", "encoding.color", "guide.legend"],
        "plan": [
          { "id": "action.createColorScale", "options": ["id", "type", "range"] },
          { "id": "action.createScatterPlot", "options": ["x", "y", "color", "guides"] }
        ],
        "unresolved": [],
        "fallbacks": []
      }
    },
    {
      "id": "val-point-appearance-encodings",
      "split": "validation",
      "stratum": "complex",
      "dataset": "mixed-observations-v1",
      "query": "Point mark with encode x, encode y, size encoding, shape encoding, opacity encoding, and axis guides.",
      "expected": {
        "constraints": ["mark.point", "encoding.x", "encoding.y", "encoding.size", "encoding.shape", "encoding.opacity", "guide.axes"],
        "plan": [
          { "id": "action.createPointMark", "options": [] },
          { "id": "action.encodeX", "options": ["field"] },
          { "id": "action.encodeY", "options": ["field"] },
          { "id": "action.encodeSize", "options": ["field"] },
          { "id": "action.encodeShape", "options": ["field"] },
          { "id": "action.encodeOpacity", "options": ["field"] },
          { "id": "action.createAxes", "options": [] }
        ],
        "unresolved": [],
        "fallbacks": []
      }
    },
    {
      "id": "val-line-error-band",
      "split": "validation",
      "stratum": "complex",
      "dataset": "temporal-groups-v1",
      "query": "Path mark with map to x, map to y, group encoding, dash encoding, an error band, and axes.",
      "expected": {
        "constraints": ["mark.line", "statistics.errorBand", "encoding.x", "encoding.y", "encoding.strokeDash", "encoding.group", "guide.axes"],
        "plan": [
          { "id": "action.createLineMark", "options": [] },
          { "id": "action.encodeX", "options": ["field"] },
          { "id": "action.encodeY", "options": ["field"] },
          { "id": "action.encodeGroup", "options": ["field"] },
          { "id": "action.encodeStrokeDash", "options": ["field"] },
          { "id": "action.createErrorBand", "options": [] },
          { "id": "action.createAxes", "options": [] }
        ],
        "unresolved": [],
        "fallbacks": []
      }
    },
    {
      "id": "val-polar-arc-pdf",
      "split": "validation",
      "stratum": "complex",
      "dataset": "polar-observations-v1",
      "query": "Arc mark with angular encoding, radial encoding, automatic guides, and export pdf.",
      "expected": {
        "constraints": ["mark.arc", "encoding.theta", "encoding.radius", "guide.all", "renderer.pdf"],
        "plan": [
          { "id": "action.createArcMark", "options": [] },
          { "id": "action.encodeR", "options": ["field"] },
          { "id": "action.encodeTheta", "options": ["field"] },
          { "id": "action.createGuides", "options": [] },
          { "id": "runtime.renderToPDF", "options": [] }
        ],
        "unresolved": [],
        "fallbacks": []
      }
    },
    {
      "id": "val-facet-shared-svg",
      "split": "validation",
      "stratum": "complex",
      "dataset": "mixed-observations-v1",
      "query": "Repeat chart by field as small multiples with x and y axes, legend, and export svg.",
      "expected": {
        "constraints": ["guide.axes", "guide.legend", "layout.facet", "renderer.svg"],
        "plan": [
          { "id": "action.facet", "options": ["field", "guides"] },
          { "id": "runtime.renderToSVG", "options": [] }
        ],
        "unresolved": [],
        "fallbacks": []
      }
    },
    {
      "id": "val-vertical-composition-png",
      "split": "validation",
      "stratum": "simple",
      "dataset": "mixed-observations-v1",
      "query": "Vertically concatenate finished views and render png.",
      "expected": {
        "constraints": ["composition.vertical", "renderer.png"],
        "plan": [
          { "id": "runtime.vconcat", "options": [] },
          { "id": "runtime.renderToPNG", "options": [] }
        ],
        "unresolved": [],
        "fallbacks": []
      }
    },
    {
      "id": "val-conflicting-legend-position",
      "split": "validation",
      "stratum": "simple",
      "dataset": "mixed-observations-v1",
      "query": "Create a legend at top while also requesting a right legend.",
      "expected": {
        "constraints": ["guide.legend", "layout.legend.top", "layout.legend.right"],
        "plan": [
          { "id": "action.createLegend", "options": [] }
        ],
        "unresolved": ["layout.legend.top", "layout.legend.right"],
        "fallbacks": ["ggaction://docs/legend-layout"]
      }
    }
  ]
}
