{
  "schemaVersion": 1,
  "id": "time-series-derivation",
  "title": "Time-Unit and Window Derivation",
  "intent": "Derive calendar units and ordered moving, ranking, lag, lead, or cumulative calculations for later chart encodings.",
  "useWhen": [
    "Use this recipe when an ordered series needs reusable temporal or window fields before marks are authored."
  ],
  "avoidWhen": [
    "Avoid this recipe when the required fields already exist or when a simple aggregate encoding fully answers the task."
  ],
  "prerequisites": [
    "Provide immutable row objects containing every field named by the selected actions and example program.",
    "Use explicit public resource identifiers whenever current program state contains more than one compatible candidate."
  ],
  "inputs": [
    {
      "name": "values",
      "description": "An array of plain row objects containing the fields required by this task.",
      "required": true
    },
    {
      "name": "options",
      "description": "Task-specific field names, resource identifiers, and supported visual or statistical choices.",
      "required": false
    }
  ],
  "outcomes": [
    "Derive calendar units and ordered moving, ranking, lag, lead, or cumulative calculations for later chart encodings."
  ],
  "steps": [
    {
      "id": "prepare-required-state",
      "instruction": "Create the data, marks, encodings, and owned resources required before authoring the time-unit and window derivation result.",
      "actions": [
        {
          "name": "createCanvas",
          "role": "supporting"
        },
        {
          "name": "createData",
          "role": "supporting"
        },
        {
          "name": "createTimeUnitData",
          "role": "supporting"
        }
      ]
    },
    {
      "id": "author-primary-result",
      "instruction": "Apply the primary action that produces the recognizable time-unit and window derivation result or workflow.",
      "actions": [
        {
          "name": "createWindowData",
          "role": "primary"
        }
      ]
    }
  ],
  "alternatives": [
    {
      "when": "Choose a neighboring recipe when its stated result and data roles match the requested chart more directly.",
      "instruction": "Prefer one complete domain facade or task recipe before composing lower-level actions manually.",
      "actions": []
    }
  ],
  "pitfalls": [
    {
      "problem": "Implicit targeting fails when several compatible datasets, marks, scales, guides, or owners exist in the same program.",
      "fix": "Pass the exact public identifier named by the action signature instead of relying on first-resource selection."
    }
  ],
  "example": {
    "path": "examples/airline-passenger-moving-windows/program.js",
    "export": "createAirlinePassengerMovingWindows"
  },
  "relatedRecipes": [],
  "docs": [
    {
      "path": "docs/api/data/window.md"
    }
  ]
}
