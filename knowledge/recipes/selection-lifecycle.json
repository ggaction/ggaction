{
  "schemaVersion": 1,
  "id": "selection-lifecycle",
  "title": "Selection and Highlight Lifecycle",
  "intent": "Select final mark items, revise the selector, apply or remove highlighting, and release stored selection state.",
  "useWhen": [
    "Use this recipe when final rendered items need a reusable visual selection and explicit cleanup lifecycle."
  ],
  "avoidWhen": [
    "Avoid this recipe when source rows should be filtered before materialization or when all mark items should change uniformly."
  ],
  "prerequisites": [
    "Provide immutable row objects containing every field named by the selected actions and example program.",
    "Use explicit public resource identifiers whenever current program state contains more than one compatible candidate."
  ],
  "inputs": [
    {
      "name": "values",
      "description": "An array of plain row objects containing the fields required by this task.",
      "required": true
    },
    {
      "name": "options",
      "description": "Task-specific field names, resource identifiers, and supported visual or statistical choices.",
      "required": false
    }
  ],
  "outcomes": [
    "Select final mark items, revise the selector, apply or remove highlighting, and release stored selection state."
  ],
  "steps": [
    {
      "id": "prepare-required-state",
      "instruction": "Prepare only the existing selection and highlight lifecycle state required by the selected focused operation.",
      "actions": [
        {
          "name": "highlightMarks",
          "role": "supporting"
        }
      ]
    },
    {
      "id": "author-primary-result",
      "instruction": "Apply the primary action that produces the recognizable selection and highlight lifecycle result or workflow.",
      "actions": [
        {
          "name": "selectMarks",
          "role": "primary"
        }
      ]
    },
    {
      "id": "revise-or-remove-owned-state",
      "instruction": "Use the focused lifecycle actions only after their owned selection and highlight lifecycle resources exist and are uniquely targeted.",
      "actions": [
        {
          "name": "editMarkSelection",
          "role": "lifecycle"
        },
        {
          "name": "removeMarkHighlight",
          "role": "lifecycle"
        },
        {
          "name": "removeMarkSelection",
          "role": "lifecycle"
        }
      ]
    }
  ],
  "alternatives": [
    {
      "when": "Choose a neighboring recipe when its stated result and data roles match the requested chart more directly.",
      "instruction": "Prefer one complete domain facade or task recipe before composing lower-level actions manually.",
      "actions": []
    }
  ],
  "pitfalls": [
    {
      "problem": "Implicit targeting fails when several compatible datasets, marks, scales, guides, or owners exist in the same program.",
      "fix": "Pass the exact public identifier named by the action signature instead of relying on first-resource selection."
    }
  ],
  "example": {
    "path": "test/llm/recipe-knowledge-examples.js",
    "export": "recipeExamples"
  },
  "relatedRecipes": [
    "mark-lifecycle"
  ],
  "docs": [
    {
      "path": "docs/api/appearance/selection-and-highlighting.md"
    }
  ]
}
