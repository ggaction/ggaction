{
  "schemaVersion": 1,
  "id": "regression-scatterplot",
  "title": "Regression Scatterplot",
  "intent": "Create a scatter plot with grouped fitted lines and optional confidence or prediction interval bands.",
  "useWhen": [
    "Use this recipe when a fitted relationship should be compared with the observed quantitative points."
  ],
  "avoidWhen": [
    "Avoid this recipe when the requested model is unsupported or when each fitted group has too few varying observations."
  ],
  "prerequisites": [
    "Provide immutable row objects containing every field named by the selected actions and example program.",
    "Use explicit public resource identifiers whenever current program state contains more than one compatible candidate."
  ],
  "inputs": [
    {
      "name": "values",
      "description": "An array of plain row objects containing the fields required by this task.",
      "required": true
    },
    {
      "name": "options",
      "description": "Task-specific field names, resource identifiers, and supported visual or statistical choices.",
      "required": false
    }
  ],
  "outcomes": [
    "Create a scatter plot with grouped fitted lines and optional confidence or prediction interval bands."
  ],
  "steps": [
    {
      "id": "prepare-required-state",
      "instruction": "Create the data, marks, encodings, and owned resources required before authoring the regression scatterplot result.",
      "actions": [
        {
          "name": "createCanvas",
          "role": "supporting"
        },
        {
          "name": "createData",
          "role": "supporting"
        },
        {
          "name": "createScatterPlot",
          "role": "supporting"
        },
        {
          "name": "filterData",
          "role": "supporting"
        },
        {
          "name": "createPointMark",
          "role": "supporting"
        },
        {
          "name": "encodeX",
          "role": "supporting"
        },
        {
          "name": "encodeY",
          "role": "supporting"
        },
        {
          "name": "encodeColor",
          "role": "supporting"
        },
        {
          "name": "encodeSize",
          "role": "supporting"
        },
        {
          "name": "encodeShape",
          "role": "supporting"
        },
        {
          "name": "encodeOpacity",
          "role": "supporting"
        },
        {
          "name": "filterMarks",
          "role": "supporting"
        },
        {
          "name": "createTextMark",
          "role": "supporting"
        },
        {
          "name": "encodeText",
          "role": "supporting"
        }
      ]
    },
    {
      "id": "author-primary-result",
      "instruction": "Apply the primary action that produces the recognizable regression scatterplot result or workflow.",
      "actions": [
        {
          "name": "createRegression",
          "role": "primary"
        }
      ]
    }
  ],
  "alternatives": [
    {
      "when": "Choose a neighboring recipe when its stated result and data roles match the requested chart more directly.",
      "instruction": "Prefer one complete domain facade or task recipe before composing lower-level actions manually.",
      "actions": []
    }
  ],
  "pitfalls": [
    {
      "problem": "Implicit targeting fails when several compatible datasets, marks, scales, guides, or owners exist in the same program.",
      "fix": "Pass the exact public identifier named by the action signature instead of relying on first-resource selection."
    }
  ],
  "example": {
    "path": "examples/cars-regression-scatterplot/program.js",
    "export": "createCarsRegressionScatterplot"
  },
  "relatedRecipes": [
    "annotations",
    "facet",
    "statistical-owner-revisions"
  ],
  "docs": [
    {
      "path": "docs/recipes/regression-scatterplot.md"
    }
  ]
}
