{
  "schemaVersion": 1,
  "id": "path-ordering",
  "title": "Path Ordering",
  "intent": "Order vertices inside each line or ranged-area series independently from source row order.",
  "useWhen": [
    "Use this recipe when repeated positions must connect according to a separate temporal or quantitative order field."
  ],
  "avoidWhen": [
    "Avoid this recipe when source row order is already canonical or when observations should remain unconnected."
  ],
  "prerequisites": [
    "Provide immutable row objects containing every field named by the selected actions and example program.",
    "Use explicit public resource identifiers whenever current program state contains more than one compatible candidate."
  ],
  "inputs": [
    {
      "name": "values",
      "description": "An array of plain row objects containing the fields required by this task.",
      "required": true
    },
    {
      "name": "options",
      "description": "Task-specific field names, resource identifiers, and supported visual or statistical choices.",
      "required": false
    }
  ],
  "outcomes": [
    "Order vertices inside each line or ranged-area series independently from source row order."
  ],
  "steps": [
    {
      "id": "prepare-required-state",
      "instruction": "Create the data, marks, encodings, and owned resources required before authoring the path ordering result.",
      "actions": [
        {
          "name": "createCanvas",
          "role": "supporting"
        },
        {
          "name": "createData",
          "role": "supporting"
        },
        {
          "name": "createLineMark",
          "role": "supporting"
        },
        {
          "name": "encodeX",
          "role": "supporting"
        },
        {
          "name": "encodeY",
          "role": "supporting"
        }
      ]
    },
    {
      "id": "author-primary-result",
      "instruction": "Apply the primary action that produces the recognizable path ordering result or workflow.",
      "actions": [
        {
          "name": "encodePathOrder",
          "role": "primary"
        }
      ]
    },
    {
      "id": "revise-or-remove-owned-state",
      "instruction": "Use the focused lifecycle actions only after their owned path ordering resources exist and are uniquely targeted.",
      "actions": [
        {
          "name": "removePathOrder",
          "role": "lifecycle"
        }
      ]
    }
  ],
  "alternatives": [
    {
      "when": "Choose a neighboring recipe when its stated result and data roles match the requested chart more directly.",
      "instruction": "Prefer one complete domain facade or task recipe before composing lower-level actions manually.",
      "actions": []
    }
  ],
  "pitfalls": [
    {
      "problem": "Implicit targeting fails when several compatible datasets, marks, scales, guides, or owners exist in the same program.",
      "fix": "Pass the exact public identifier named by the action signature instead of relying on first-resource selection."
    }
  ],
  "example": {
    "path": "examples/gapminder-development-trajectories/program.js",
    "export": "createGapminderDevelopmentTrajectories"
  },
  "relatedRecipes": [
    "ranged-and-specialized-encodings"
  ],
  "docs": [
    {
      "path": "docs/recipes/path-ordering.md"
    }
  ]
}
